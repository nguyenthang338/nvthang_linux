#To roll back to a previous commit while keeping the changes
git reset --soft 789abcd


#To roll back to last commit
#git reset --soft HEAD~

# Loai bo vinh vien moi thay doi dc thuc hien sau khi cam ket cu the 
#git reset --hard 789abcd

# loai bo vinh vien moi thay doi dc thuc hien sau khi last commit . 
#git reset --hard HEAD~
